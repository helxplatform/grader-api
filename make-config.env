# Makefile configuration variables

BASE_IMAGE=containers.renci.org/helxplatform/grader-api-poetry-base
BASE_IMAGE_TAG=v1.0.3

APP_NAME=grader-api
IMAGE_REPO=containers.renci.org/helxplatform
TAG=test-poetry-11

# These variables used when running the container.

CONTAINER_PORT=8080
FORWARDING_PORT=8080
# ENTRYPOINT=
# UID=0
# UID=1000
UID=1015180002
GID=0
HOST_MOUNT="-v $PWD/host:/host"
